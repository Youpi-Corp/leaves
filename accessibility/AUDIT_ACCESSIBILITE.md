📘 Accessibility Audit (internal tool)

Goal:
Enable any developer to quickly analyze accessibility on all pages (uses sitemap when available).

Install:

npm install puppeteer axe-core

Run:

npm run audit:a11y

Result:

Summary in the console

File accessibility/reports/accessibility-report.json containing detailed violations
